将NIS-Elements的tracking的数据导出为CSV。运行此宏，选好文件后，会弹出对话框，指定以下参数：
1）x，y座标以及时间time对应的列。
2) 时间支持两种格式：0.00(s) 或是 00:00:00
3) 请求输入计算MSD延时所需帧数MSD delay number
4) 输出时左上角和右下角显示对应的坐标值。时间标尺在最右边。MSD值分两种显示，第一行是以延时帧数为单位，第二行以秒为单位。
